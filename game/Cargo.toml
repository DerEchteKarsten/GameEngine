[package]
name = "game"
version.workspace = true
edition.workspace = true

[dependencies]
core = { version = "0.1.0", path = "../core" }
glam = {workspace = true}
bevy = {workspace = true}

[features]
default = ["dynamic_linking"]
dynamic_linking = ["core/dynamic_linking", "bevy/dynamic_linking"]
